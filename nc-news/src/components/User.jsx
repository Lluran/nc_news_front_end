import React from 'react';
import PropTypes from 'prop-types';

const User = props => {
  return (
    <section>
      <img src='' alt='user avatar' />
      <p>Username</p>
      <p>Name</p>
    </section>
  );
};

User.propTypes = {
  
};

export default User;